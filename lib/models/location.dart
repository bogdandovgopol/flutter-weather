class Location {
  double latitude;
  double longitude;
  String city;
  String country;

  //constructor, populate the object with provided data
  Location(this.latitude, this.longitude, this.city, this.country);
}
